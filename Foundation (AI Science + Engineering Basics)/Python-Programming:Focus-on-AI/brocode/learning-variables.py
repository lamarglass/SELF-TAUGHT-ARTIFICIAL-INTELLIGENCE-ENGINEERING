# Documented-By-Lamar
from name_art import print_name

print_name() 

# Variables = Is a container for a value (string, integer, float, boolean)
#             A variables behaves as if it was the value it contains

first_name = input("Enter your first name: ") # the 'first_name' word/code is a variable, meaning it behaves as if it its the word/code 'Lamar'

print(first_name * 10) # code 'print' is used to display the out come assigned to it.
print(f"Hello there {first_name}:)")
# f-string also known as 'formatted string literals is also use to dsplay assigned code.

food = input("Enter your favourite food: ")
print(f"you like {food} " * 10)